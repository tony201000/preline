rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Seuls les utilisateurs authentifiés peuvent lire et écrire dans Firestore
    match /figurines/{figurineId} {
      allow read, write: if request.auth != null;
    }
  }
}